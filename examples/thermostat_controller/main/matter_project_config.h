#pragma once

// Overrides the generic connectedhomeip defaults ("TEST_PRODUCT" / "TEST_VENDOR") with
// names that identify this example in commissioners/apps (chip-tool, HomeKit-style
// controllers, etc.).
#define CHIP_DEVICE_CONFIG_DEVICE_PRODUCT_NAME "Thermostat Controller"
#define CHIP_DEVICE_CONFIG_DEVICE_VENDOR_NAME "Espressif Systems"
